import React from 'react';

const ExampleDisplay = () => {
    return (
        <div>
            I AM THE ExampleDisplay!
            I will hold the example user that users can walk through if they haven't registered or logged in to see what the app does.
        </div>
    )
}

export default ExampleDisplay;